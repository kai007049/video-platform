brokerClusterName=DefaultCluster
brokerName=broker-a
brokerId=0
deleteWhen=04
fileReservedTime=48
brokerRole=ASYNC_MASTER
flushDiskType=ASYNC_FLUSH
# Local development: advertise the current host LAN IP so the backend process
# on the host can reach the published broker port through NameServer routing.
brokerIP1=10.142.121.95
# Allow auto topic creation in local development to reduce first-send failures.
autoCreateTopicEnable=true
